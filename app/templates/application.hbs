<nav>
  {{#link-to "index"}}
    Index
  {{/link-to}}
  {{#if session.isAuthenticated}}
    <span>{{sessionAccount.currentUser.email}}</span>
    {{#link-to "dashboard"}}
      Dashboard
    {{/link-to}}
    <button {{action "logout"}}>Logout</button>
  {{else}}
    {{#link-to "login"}}
      Login
    {{/link-to}}
    {{#link-to "signup"}}
      Sign up
    {{/link-to}}
  {{/if}}
</nav>
{{outlet}}
