<form {{action 'createHome' on='submit'}}>
  <div>
    <label>Color</label>
    {{input type='text' value=model.color}}
  </div>
  <div>
    <input type='submit' value='Save'/>
    <button {{action 'cancel'}}>Cancel</button>
  </div>
</form>
