import { filterArray } from 'housing/helpers/filter-array';
import { module, test } from 'qunit';

module('Unit | Helper | filter array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterArray([42]);
  assert.ok(result);
});
